
;色っぽい仕草
@EVENT_COUNTER_MESSAGE10
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 요염한 몸짓으로 유혹해 왔다…
RETURN 1

;바라보기
@EVENT_COUNTER_MESSAGE11
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 촉촉한 눈빛으로 바라보고있다…
RETURN 1

;몸 밀착
@EVENT_COUNTER_MESSAGE12
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%에게 몸을 밀착시켰다…
RETURN 1

;입김불기
@EVENT_COUNTER_MESSAGE13
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER% 뒤로 살금살금 다가와 귓가에 뜨거운 숨을 불어넣었다…
RETURN 1

;엉덩이 쓰다듬기
@EVENT_COUNTER_MESSAGE14
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 엉덩이를 쓰다듬었다…
RETURN 1



;키스
@EVENT_COUNTER_MESSAGE30
IF ABL:TARGET:기교 > 3
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 얼굴에 다가와、갑자기 입술을 빼았고 끈적끈적한 혀놀림으로 입안을 유린했다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 얼굴에 다가와、갑자기 입술을 빼았았다…
ENDIF
RETURN 1

;뒤에서 껴안기
@EVENT_COUNTER_MESSAGE31
PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% 뒤에서 %조사처리(CALLNAME:TARGET,"를")% 껴안았다
SIF !TALENT:TARGET:110
	PRINTFORML %CALLNAME:PLAYER%의 등 뒤에서 %CALLNAME:TARGET%의 부드러운 가슴이 밀착됐다…
RETURN 1

;앞에서 껴안기
@EVENT_COUNTER_MESSAGE32
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 껴안았다
PRINTFORML 숨을 헐떡이며 몸을 껴안아왔다…

RETURN 1

;스킨쉽
@EVENT_COUNTER_MESSAGE33
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 손을 잡아、자신의 가슴으로 이끌었다
PRINTFORML %CALLNAME:TARGET%의 심장 박동이 손에서 전해져 온다…
RETURN 1


;뒤에서 가슴 주물럭
@EVENT_COUNTER_MESSAGE36
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 등 뒤에서 가슴을 주물러 왔다…
RETURN 1

;수음
@EVENT_COUNTER_MESSAGE50
IF HAVE_PENIS(PLAYER)
	IF BASE:PLAYER:사정 > MAXBASE:PLAYER:2*2/3
		IF SELECTCOM == 501 || SELECTCOM == 508
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 얼굴에 걸친 채
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 얼굴을 바라보며
		ENDIF
		IF TALENT:TARGET:새드
			PRINTFORML 격렬하게 페니스를 조여 충분히 조급하게 만든 뒤 사정을 유도했다…
		ELSE
			PRINTFORML 격렬하게 페니스를 쥐어짜 사정으로 이끌었다…
		ENDIF
	ELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF SELECTCOM == 501 || SELECTCOM == 508
			PRINTFORM %CALLNAME:MASTER%의 얼굴에 걸친 채
		PRINTFORML 노출된 페니스에 손가락을 감아 문지르기 시작했다…
	ENDIF
ELSE
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")%
	PRINTFORML 드러난 %CALLNAME:PLAYER%의 보지를 손가락으로 더듬었다・・・
ENDIF
RETURN 1

;입淫
@EVENT_COUNTER_MESSAGE51
IF HAVE_PENIS(PLAYER)
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")%
	PRINTFORM 노출된 페니스를
	IF ABL:TARGET:기교 > 3
		PRINTFORML 입에 물고 능숙한 입놀림으로 빨기 시작했다…
	ELSE
		PRINTFORML 입에 물고 빨기 시작했다…
	ENDIF
ELSE
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")%
	IF TEQUIP:PLAYER:속옷（하） || TEQUIP:PLAYER:전신속옷
		PRINTFORML %CALLNAME:PLAYER%의 속옷을 벗기고、
	ENDIF
	PRINTFORML 드러난 %CALLNAME:PLAYER%의 보지에 혀를 넣었다…
ENDIF
RETURN 1

;스마타
@EVENT_COUNTER_MESSAGE52
IF HAVE_PENIS(PLAYER)
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%에게 올라타 페니스를 보지에 문지르기 시작했다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 두 사람의 성기를 겹쳐서 문지르기 시작했다…
ENDIF
RETURN 1

;파이즈리
@EVENT_COUNTER_MESSAGE53
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 페니스를
PRINTFORML 가슴에 끼고 문지르기 시작했다…
RETURN 1

;풋잡
@EVENT_COUNTER_MESSAGE54
IF TALENT:TARGET:새드
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 미소지으며 %CALLNAME:PLAYER%의 페니스를 밟고 문지르기 시작했다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 페니스를 밟고 문지르기 시작했다…
ENDIF
RETURN 1

;애널애무
@EVENT_COUNTER_MESSAGE55
IF TALENT:TARGET:새드
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 애널에 손가락을 비틀어 애무했다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 애널을 손가락으로 애무했다…
ENDIF
RETURN 1

;애널핥기
@EVENT_COUNTER_MESSAGE56
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 애널을 정성스럽게 핥기 시작했다…
RETURN 1

;손가락 삽입
@EVENT_COUNTER_MESSAGE57
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 보지에 천천히 손가락을 삽입했다…
RETURN 1

;가슴애무
@EVENT_COUNTER_MESSAGE58
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의
IF HAVE_VAGINA(PLAYER)
	PRINTFORML 가슴을 주물러주었다…
ELSE
	PRINTFORML 젖꼭지를 손가락으로 꼬집어 만지작거리기 시작했다…
ENDIF
RETURN 1

;유두吸い
@EVENT_COUNTER_MESSAGE59
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의
PRINTFORML 젖꼭지를 빨아먹었다…
RETURN 1

;ディープキス
@EVENT_COUNTER_MESSAGE60
IF ABL:TARGET:기교 > 3
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 얼굴을 잡고 입맞춤하며 열정적으로 혀을 얽고 침을 흘려왔다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 얼굴을 잡고 입맞춤하며 열정적으로 혀를 휘감아왔다…
ENDIF
RETURN 1

;커닐링구스강제
@EVENT_COUNTER_MESSAGE70
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 얼굴에 천천히 걸쳐 입가에 보지를 눌렀다…
RETURN 1

;펠라치오強制
@EVENT_COUNTER_MESSAGE71
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 입에 페니스를 넣었다…
RETURN 1

;애널봉사
@EVENT_COUNTER_MESSAGE72
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 얼굴에 걸쳐 입가에 애널을 눌렀다…
RETURN 1

;파후파후
@EVENT_COUNTER_MESSAGE73
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 얼굴을 안고 가슴을 밀어붙였다…
RETURN 1

;가슴 맞대기
@EVENT_COUNTER_MESSAGE74
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 몸에 가슴을 눌러왔다…
RETURN 1

;タワシ洗い
@EVENT_COUNTER_MESSAGE75
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 팔에 성기를 문지르고있다…
RETURN 1

;삽입됨
@EVENT_COUNTER_MESSAGE80
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 보지에 페니스를 박고、허리를 움직이기 시작했다…
IF TALENT:PLAYER:처녀 && TALENT:2 >= 1
	PRINTFORML 결합부에서는 %CALLNAME:PLAYER%의 파과혈이 흘러나오고 있다…
ELSE
	PRINTFORML 결합부에서는 %조사처리(CALLNAME:TARGET,"과")% %CALLNAME:PLAYER%의 체액이 흘러나오고있다…
ENDIF
RETURN 1

;후배위애널
@EVENT_COUNTER_MESSAGE82
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 앞으로 엎드리게 한 뒤 
PRINTFORM %CALLNAME:PLAYER%의、
IF PALAM:PLAYER:윤활 < PALAMLV:2
	PRINTFORML 축축하지 않은 애널에 강제로 페니스를 밀어넣었다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%의 애널에 페니스를 삽입했다…
ENDIF
RETURN 1
;---------------------------------------------------------------------------
;배면좌위애널
@EVENT_COUNTER_MESSAGE83
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 뒤에서 껴안으면서 
PRINTFORM %CALLNAME:PLAYER%의、
IF PALAM:PLAYER:윤활 < PALAMLV:2
	PRINTFORML 축축하지 않은 애널에 강제로 페니스를 밀어넣었다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%의 애널에 페니스를 삽입했다…
ENDIF
RETURN 1
;--------------------------------------------------------------------------
;기승위애널
@EVENT_COUNTER_MESSAGE81
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 누워서、%CALLNAME:PLAYER%에게 스스로 페니스를 물도록 명령했다。
PRINTFORM %조사처리(CALLNAME:PLAYER,"는")%
IF PALAM:PLAYER:윤활 < PALAMLV:2
	PRINTFORML 고통을 참아가며 축축한 애널에 페니스를 맞이했다…
ELSE
	PRINTFORML 자신의 애액을 윤할제 삼아 애널에 페니스를 맞이했다…
ENDIF
RETURN 1
;---------------------------------------------------------------------------
;;기승위 
@EVENT_COUNTER_MESSAGE91
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 위에 천천히 걸쳐앉았다…
RETURN 1
;----------------------------------------------------------------------------
;질성교
@EVENT_COUNTER_MESSAGE500_1
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 질벽을 조여 %CALLNAME:PLAYER%의 페니스를 쥐었다…
RETURN 1
;----------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE500_2
;다이슈키홀드
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 껴안으면서 두 다리로 허리를 감싸안았다…
RETURN 1
;----------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE500_3
;성교 과시
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 상체를 돌려 %CALLNAME:PLAYER%에게 결합부를 보여주면서 페니스를 쥐고있다…

RETURN 1
;--------------------------------------------------------------------------------
;수음
@EVENT_COUNTER_MESSAGE502
IF NOWEX:PLAYER:사정
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 넘쳐흐르는 정액을 페니스 전체에 퍼뜨리고 단번에 속도를 높에 페니스를 쥐어짰다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 페니스를 잡고 천천히 위아래로 움직이고있다…
ENDIF
RETURN 1
;---------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE503_1
;펠라치오
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 페니스를 물고、입술로 귀두를 쥐고있다…
RETURN 1
;---------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE503_2
;딥 스롯
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 한계까지 발기한 %CALLNAME:MASTER%의 페니스를 뿌리까지 삼켜버렸다
PRINTFORML 격렬하게 머리를 움직이고 있다…
RETURN 1
;------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE504_1
;파이즈리
IF !TALENT:TARGET:110
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 페니스를 가슴에 끼고있다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 페니스에 젖꼭지를…
ENDIF
RETURN 1
;------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE504_2
;세로파이즈리,젖성교
PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% %CALLNAME:TARGET%의 풍만한 가슴 틈으로 페니스를 집어넣고 허리를 흔들었다…
RETURN 1
;------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE504_3
;계곡만들기
IF TALENT:TARGET:110
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 평평한 가슴을 열심히 끌어당겨 어떻게든 페니스를 끼우려고 하고있다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 가슴을 모아 만든 틈새에 페니스를 끼우고 상체를 격렬하게 움직이고있다…
ENDIF
RETURN 1
;---------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE505_1
;스마타
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 허벅지로 %CALLNAME:MASTER%의 페니스를 끼우고 허리를 움직였다…
RETURN 1
;---------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE505_2
;수음추가
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 허벅지로 %CALLNAME:MASTER%의 페니스를 끼우고 귀두를 손가락으로 자극했다…
RETURN 1
;------------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE600_1
;구슬 주무르기
IF TALENT:TARGET:새드
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 음낭에 손을 뻗어 주물러 왔다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 음낭에 손을 뻗어 부드럽게 주물렀다…
ENDIF
RETURN 1
;------------------------------------------------------------------------------------------
@EVENT_COUNTER_MESSAGE600_2
PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 음낭을 입에 물고 입안에서 구슬을 굴리고있다
RETURN 1
;------------------------------------------------------------------------------------------
;애널애무
@EVENT_COUNTER_MESSAGE601
IF ABL:PLAYER:Ａ감각 < 2
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 애널을 손가락으로 자극하고 있다…
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 애널에 손가락을 삽입하여 직장까지 휘저었다…
ENDIF
RETURN 1
;-------------------------------------------------------------------------------------------
