;==========================================================
;Aion식소환술 용, 그 슬롯이 사용가능한지 보는 함수
;ARG 대상 캐릭터
;ARG:1 사용되는 스킬 슬롯
;RETURN -1⇒Aion식 소환술이 아님　0⇒사용불능　1⇒사용가능
;==========================================================
@Aion식소환술_스킬枠판정(ARG, ARG:1)
#FUNCTION
;そもそも素質を持っていない
SIF TALENT:ARG:Aion식소환술
	RETURNF -1
;存在しない스킬枠を指定した
SIF ARG:1 < 1
	RETURNF 0
;リンク先の스킬枠を指定した
SIF ARG:1 > 4
	RETURNF 0
RETURNF 1





;==========================================================
;Aion식소환술用、스킬を인간시스킬に格納する処理
;（ここに自前の스킬を保存。何かあるたびに復元している）
;ARG 대상のキャラ
;==========================================================
@Aion식소환술_인간시스킬반영(ARG)
#DIM ループ用
FOR ループ用, 1, 5
	ABL:ARG:@"인간시스킬{ループ用}" = ABL:ARG:@"스킬{ループ用}"
NEXT
