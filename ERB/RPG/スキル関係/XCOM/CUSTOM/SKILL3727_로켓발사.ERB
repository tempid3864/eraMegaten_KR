;XCOM2 스파크비트 기본기 + EXO 아머 기술

;스킬名
@SKILL_NAME_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 로켓발사

;　명중
@SKILL_HITRATE_3727, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 200

;　위력
@SKILL_POWER_3727, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 70
IF BASE:ARG:LV >= 30
	RETURN 70
ELSEIF BASE:ARG:LV >= 25
	RETURN 60
ELSEIF BASE:ARG:LV >= 20
	RETURN 50
ELSEIF BASE:ARG:LV >= 15
	RETURN 40
ELSE
	RETURN 40
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;척탄병 훈련을 받았을 때.
IF CFLAG:(ARG:1):척탄병_훈련 == 1
	RETURN 2
;레벨 35에 EXO 아머와 척탄병 장비(전자기 캐넌)를 장비하고 있을때
ELSEIF EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708 && EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):총 == 2643 && BASE:(ARG:1):LV > 34
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;척탄병 훈련을 받았을 때.
IF CFLAG:(ARG:1):척탄병_훈련 == 1
	RETURN 2
;레벨 35에 EXO 아머와 척탄병 장비(전자기 캐넌)를 장비하고 있을때
ELSEIF EQUIP:(ARG:1):팔 == 4080 && EQUIP:(ARG:1):발 == 4708 && EQUIP:(ARG:1):몸통 == 3813 && EQUIP:(ARG:1):총 == 2643 && BASE:(ARG:1):LV > 34
	RETURN 2
ELSE
	RETURN 1
ENDIF

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_3727, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;消費なし＝1

;　비용의 양
@SKILL_COST_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 18
IF BASE:ARG:LV >= 30
	RETURN 18
ELSEIF BASE:ARG:LV >= 25
	RETURN 15
ELSEIF BASE:ARG:LV >= 20
	RETURN 12
ELSEIF BASE:ARG:LV >= 15
	RETURN 9
ELSE
	RETURN 6
ENDIF

;　상성
@SKILL_TYPE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;스킬 치명타율
@SKILL_CRITICAL_RATE_3727, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 5

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_3727, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 폭발계 만능. 물리

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_3727, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_3727
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_3727
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_3727
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 20
;만능

;계승불능
@계승불능_3727
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_3727, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4

;　구상의 표시와 그 타입
@SKILL_MESSAGE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3727
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_3727
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과 기본 부여 확률
@SKILL_ADDTIONAL_BASE_RATE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과 부여확률 상한
@SKILL_ADDTIONAL_MAX_RATE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;확산 공격용 추가처리
@SKILL_SPREAD_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;확산거리:1

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_3727,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
CALL ATTACK_SPREAD,ARG,ARG:1,3727
;====================================================
;실행내용 여기까지
;====================================================
;　 설명문 표시
@SKILL_EXPLAIN_3727
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL E.X.O 아머 슈트나 SPARK 비트로 사용할 수 있는 로켓 발사기.
PRINTL 적 1체와 그 주변에 로켓을 발사한다. 물리판정.

;계승에 필요한 부위
@계승부위_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3727,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;특별히 없음

