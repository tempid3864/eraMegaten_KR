;XCOM2 WOTC 워록 공격기.

;스킬명
@SKILL_NAME_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 교란소총사격

;　명중
@SKILL_HITRATE_173, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 160
IF BASE:ARG:LV >= 40
	RETURN 160
ELSEIF BASE:ARG:LV >= 35
	RETURN 140
ELSEIF BASE:ARG:LV >= 30
	RETURN 120
ELSEIF BASE:ARG:LV >= 20
	RETURN 115
ELSEIF BASE:ARG:LV >= 10
	RETURN 110
ELSE
	RETURN 110
ENDIF

;　위력
@SKILL_POWER_173, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 195
IF BASE:ARG:LV >= 40
	RETURN 195
ELSEIF BASE:ARG:LV >= 35
	RETURN 185
ELSEIF BASE:ARG:LV >= 30
	RETURN 145
ELSEIF BASE:ARG:LV >= 20
	RETURN 125
ELSEIF BASE:ARG:LV >= 10
	RETURN 115
ELSE
	RETURN 115
ENDIF

;　흡수율
;@SKILL_HP_ABSORBER_173, ARG
;#LOCALSIZE 2
;#LOCALSSIZE 2
;RETURN 100

;　최저공격횟수
@SKILL_MINATTACKNUMBER_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_173, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;소모없음＝1

;　비용의 양
@SKILL_COST_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;　상성
@SKILL_TYPE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 17
;만능

;스킬 치명타율
@SKILL_CRITICAL_RATE_173, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 10

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_173, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;EXTRA. 사격계 특수스킬

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_173, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_173
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 2

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_173
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_173
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;상성은 만능이지만 비구계로 계승함.

;계승불능
@계승불능_173
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도수정
@SKILL_SPEED_173, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 15

;　구상의 표시와 그 타입
@SKILL_MESSAGE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_173
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_173
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과 기본 부여 확률
@SKILL_ADDTIONAL_BASE_RATE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과 부여확률 상한
@SKILL_ADDTIONAL_MAX_RATE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_173,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)

LOCAL:1 = BASE:LOCAL:ＨＰ
CALL ATTACK_SINGLE,ARG,LOCAL,165
LOCAL:1 = MAX(0, LOCAL:1 - BASE:LOCAL:ＨＰ)
SIF LOCAL:1 + BASE:ARG:ＨＰ >= MAXBASE:ARG:ＨＰ
	LOCAL:1 = MAXBASE:ARG:ＨＰ - BASE:ARG:ＨＰ

IF LOCAL:1 > 0
	PRINTFORML 강점 '영혼강탈자' 발동!
	PRINTFORMW 　　TARGET:[{CFLAG:ARG:포지션}] %CALLNAME:ARG,20,LEFT%　>>>>>>　{LOCAL:1}회복

	CALL VAR_HP, ARG , LOCAL:1, 3
ENDIF

;====================================================
;실행내용 여기까지
;====================================================
;　 설명문 표시
@SKILL_EXPLAIN_173
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL XCOM2 WOTC 워록의 기본공격. 스킬로 분류한다. 영혼강탈자 특성에 의해 체력도 회복한다.
PRINTL 사실상 워록의 유일한 생존기이며, 치명타율이 높다.

;사용조건
@SKILL_SPECIAL_ACTIONABLE_173,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;특별히 없음