;XCOM2 WOTC 리퍼 초기 단계 스킬.
;철저히 레벨에 비례한 공격력을 입힌다.

;스킬명
@SKILL_NAME_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RESULTS = 원격작동

;　명중
@SKILL_HITRATE_193, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 105
IF BASE:ARG:LV >= 40
	RETURN 105
ELSEIF BASE:ARG:LV >= 30
	RETURN 95
ELSEIF BASE:ARG:LV >= 20
	RETURN 85
ELSEIF BASE:ARG:LV >= 10
	RETURN 75
ELSE
	RETURN 75
ENDIF

;　위력
@SKILL_POWER_193, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 120
IF BASE:ARG:LV >= 40
	RETURN 120
ELSEIF BASE:ARG:LV >= 30
	RETURN 100
ELSEIF BASE:ARG:LV >= 20
	RETURN 80
ELSEIF BASE:ARG:LV >= 10
	RETURN 60
ELSE
	RETURN 60
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　ＨＰ를 소모하면 2 ＭＰ를 소모하면 3
@SKILL_COSTTYPE_193, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3
;소모없음＝1

;　비용의 양
@SKILL_COST_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 11
IF BASE:ARG:LV >= 40
	RETURN 11
ELSEIF BASE:ARG:LV >= 30
	RETURN 9
ELSEIF BASE:ARG:LV >= 20
	RETURN 7
ELSEIF BASE:ARG:LV >= 10
	RETURN 5
ELSE
	RETURN 5
ENDIF

;　상성
@SKILL_TYPE_193,ARG
#LOCALSSIZE 3
#LOCALSIZE 4

SIF ARG < 0 || 스킬속성표시중
	RETURN GET_TYPE_NUM("비구")
SIF CFLAG:ARG:타겟 > 16 || CFLAG:ARG:타겟 < 1
	RETURN GET_TYPE_NUM("비구")
LOCAL = POS(CFLAG:ARG:타겟)
SIF LOCAL < 0
	RETURN GET_TYPE_NUM("비구")
LOCAL:2 = -300
LOCALS = 비구
LOCALS:1 = 화염
LOCALS:2 = 비구
FOR LOCAL:1, 1, 3
	;判定用に数値を仕舞う。反射は-200扱い
	LOCAL:3 = MAXBASE:LOCAL:(LOCALS:(LOCAL:1)) == 999 ? -200 # MAXBASE:LOCAL:(LOCALS:(LOCAL:1))
	LOCAL:3 = CFLAG:LOCAL:(LOCALS:(LOCAL:1)+"무효화횟수") ? 0 # LOCAL:3
	IF LOCAL:3 > LOCAL:2
		LOCAL:2 = LOCAL:3
		LOCALS = %LOCALS:(LOCAL:1)%
	ENDIF
NEXT
RETURN GET_TYPE_NUM(LOCALS)
;비구

;　EXTRA면 1, MAGIC이면 2
@SKILL_DECIDE_TYPE_193, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;どちらでもない

;　물리 타격 판정 1 마법 타격 판정2
@SKILL_DAMAGETYPE_193, ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　공격대상 : 1적 2아군 3 전체 4특수
@SKILL_TARGET_193
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　사거리
@SKILL_RANGE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 효과타입　1공격、2회복、3상태변화、4특수、5상태회복
@SKILL_EFECT_193
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_193
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;비구

;계승불능
@계승불능_193
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1

;속도보정
@SKILL_SPEED_193, ARG = -1
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 4

;　구상의 표시와 그 타입
@SKILL_MESSAGE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_193
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_193
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN GET_TYPE_NUM("화염")

;추가효과 기본 부여 확률
@SKILL_ADDTIONAL_BASE_RATE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;추가효과 부여확률 상한
@SKILL_ADDTIONAL_MAX_RATE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 0

;확산 공격용 추가처리
@SKILL_SPREAD_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
SIF ARG == -1
	RETURN 2
IF BASE:ARG:LV >= 30
	RETURN 2
ELSEIF BASE:ARG:LV >= 10
	RETURN 1
ELSE
	RETURN 1
ENDIF
;확산거리:1. 레벨 30 이상일 때 2. 먹힘!

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_193,ARG,ARG:1
#LOCALSIZE 2
#LOCALSSIZE 2
LOCAL = POS(ARG:1)
CALL ATTACK_SPREAD,ARG,ARG:1,193


;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_193
#LOCALSIZE 2
#LOCALSSIZE 2
PRINTL 리퍼들이 사용하는 원격 폭발물 작동 스킬.
PRINTL 적 1체와 그 주변에 비구+화염 복합 상성의 피해를 입힌다。
PRINTL 적이 BOMB 상태라면 폭발시킬 수 있으며 레벨 30부터 확산 범위가 넓어진다.

;사용조건
@SKILL_SPECIAL_ACTIONABLE_193,ARG
#LOCALSIZE 2
#LOCALSSIZE 2
RETURN 1
;특별히 없음

