;
;スキルのテンプレートになっています

;スキル名
@SKILL_NAME_3551,ARG
;修羅
;SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	RESULTS = 루푸시ㆍ테크ㆍ눔ㆍ치우
;羅刹
SIF ARG > -1 && SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
	RESULTS = 루푸시ㆍ테크ㆍ눔ㆍ야크

;　명중
@SKILL_HITRATE_3551, ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 70
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 70
ENDIF
RETURN 70

;　위력
@SKILL_POWER_3551, ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 200
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 200
ENDIF
RETURN 200

;　최저공격횟수
@SKILL_MINATTACKNUMBER_3551,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_3551,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_3551, ARG
RETURN 3
;消費なし＝1

;　코스트량
@SKILL_COST_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 27
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 20
ENDIF
RETURN 27

;　상성
@SKILL_TYPE_3551,ARG
RETURN 5

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_3551, ARG
RETURN 1
;どちらでもない

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_3551, ARG
LOCAL = SKILL_USE_CHARA()
IF CHARANUM_CHECK(LOCAL) > -1
	;修羅
	SIF SKILLGAGE_F_GET(LOCAL,[[스킬:분노폭발]]) == 0
		RETURN 2
	;羅刹
	SIF SKILLGAGE_F_GET(LOCAL,[[스킬:분노폭발]]) == 1
		RETURN 1
ENDIF
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_3551
RETURN 1

;　사거리
@SKILL_RANGE_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 2
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 2
ENDIF
RETURN 2

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 1
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 1
ENDIF
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_3551
RETURN 1

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_3551,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_3551
RETURN 5

;속도보정
@SKILL_SPEED_3551, ARG = -1
RETURN 0

;　口上の表示とそのタイプ
@SKILL_MESSAGE_3551,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_3551
RETURN 0

;스킬변화？용 랭크
@SKILL_RANK_3551
RETURN 23

@SKILL_RANKUP_3551
RETURN 0


;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 6
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 5
ENDIF
RETURN 6

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 8
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 5
ENDIF
RETURN 8

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 50
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 50
ENDIF
RETURN 50

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_3551,ARG
IF ARG > -1
	;修羅
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
		RETURN 95
	;羅刹
	SIF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 1
		RETURN 95
ENDIF
RETURN 95

;====================================================
;ダメージ発動時
;====================================================
@SKILL_DAMAGE_EFFECT_3551,ARG,ARG:1
;修羅
IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	PRINT  & 마법위력、마법효과 저하
	CFLAG:(ARG:1):마법위력강화 = MAX(-16, CFLAG:(ARG:1):마법위력강화 - 4)
	CFLAG:(ARG:1):마법효과강화 = MAX(-16, CFLAG:(ARG:1):마법효과강화 - 4)
;羅刹
ELSE
ENDIF

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_3551,ARG,ARG:1
LOCAL = POS(ARG:1)
;修羅
IF SKILLGAGE_F_GET(ARG,[[스킬:분노폭발]]) == 0
	CALL RATE_ATTACK_SINGLE,ARG,LOCAL,3551,-1,400
;羅刹
ELSE
	CALL RATE_ATTACK_SINGLE,ARG,LOCAL,3551,-1,300
	CALL ATTACK_SINGLE,ARG,LOCAL,3551
ENDIF



;====================================================
;실행내용 여기까지
;====================================================


;　 説明文の表示
@SKILL_EXPLAIN_3551
IF TFLAG:전투중 == 0 && U > -1 && HAVE_SKILL(U, [[스킬:분노폭발]])
	PRINTL 【수라】
	PRINTL 적 1체에 20％ 빙결 비율데미지+BIND+마법효과、마법위력 저하
	PRINTL 【나찰】
	PRINTL 적 1체에 20％ 빙결 비율데미지+FREEZE、빙결 물리데미지
ELSEIF U < 0 || SKILLGAGE_F_GET(U,[[스킬:분노폭발]]) == 0
	PRINTL 얼리는 손바닥 찌르기　상대를 얼려 들리받는다
	PRINTL 적 1체에 빙결 속성으로 대상의 현재 HP의 20％ 데미지(상한400)를 준다。명중률이 낮다
	PRINTL 추가효과：BIND、마법위력ㆍ마법효과 저하
ELSEIF SKILLGAGE_F_GET(U,[[스킬:분노폭발]]) == 1
	PRINTL 얼리는 손바닥 부수기　상대를 얼려 추격한다
	PRINTL 적 1체에 빙결 속성으로 대상의 현재 HP의 20％ 데미지(상한300)를 주고、FREEZE 상태로 만든다。명중률이 낮다
	PRINTL 그 뒤、빙결 속성으로 중위력의 물리공격을 행한다
ENDIF


;계승에 필요한 부위
@계승부위_3551,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_3551,ARG
RETURN 1
;とくにない

