

;스킬명
@SKILL_NAME_メテオールㆍシュトラール,ARG
RESULTS = 메테올ㆍ슈트랄

;　명중
@SKILL_HITRATE_メテオールㆍシュトラール, ARG
RETURN 100

;　위력
@SKILL_POWER_メテオールㆍシュトラール, ARG
RETURN 300

;;クリティカル率
;@SKILL_CRITICAL_RATE_メテオールㆍシュトラール, ARG
RETURN 5

;;　吸収率
@SKILL_HP_ABSORBER_メテオールㆍシュトラール, ARG
IF CFLAG:ARG:타인기합플래그 > 0
	RETURN 50
ELSE
	RETURN 0
ENDIF
;　최저공격횟수
@SKILL_MINATTACKNUMBER_メテオールㆍシュトラール,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_メテオールㆍシュトラール,ARG
RETURN 1


;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_メテオールㆍシュトラール,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_メテオールㆍシュトラール,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_メテオールㆍシュトラール, ARG
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_メテオールㆍシュトラール,ARG
RETURN 25

;　상성
@SKILL_TYPE_メテオールㆍシュトラール,ARG
RETURN 5


;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_メテオールㆍシュトラール,ARG
RETURN 1

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_メテオールㆍシュトラール,ARG
RETURN 1


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_メテオールㆍシュトラール,ARG
RETURN 1

;　사거리
@SKILL_RANGE_メテオールㆍシュトラール,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_メテオールㆍシュトラール,ARG
	RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_メテオールㆍシュトラール,ARG
RETURN 1

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_メテオールㆍシュトラール,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_メテオールㆍシュトラール,ARG
;RETURN 23
;剣撃

;속도보정
@SKILL_SPEED_メテオールㆍシュトラール, ARG = -1
RETURN 0

;;　구상의 표시와 그 타입
;@SKILL_MESSAGE_メテオールㆍシュトラール,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_メテオールㆍシュトラール, ARG = -1
RETURN 0

;没
;@SKILL_RANK_メテオールㆍシュトラール
;@SKILL_RANKUP_メテオールㆍシュトラール
;@계승불능_メテオールㆍシュトラール

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_メテオールㆍシュトラール,ARG
RETURN GET_STATE_NUM("DYING")

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_メテオールㆍシュトラール,ARG
RETURN 5
;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_メテオールㆍシュトラール,ARG
RETURN 40

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_メテオールㆍシュトラール,ARG
RETURN 100

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_メテオールㆍシュトラール,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_メテオールㆍシュトラール,ARG,ARG:1
;RETURN 0

;====================================================
;화조ンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_メテオールㆍシュトラール,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_メテオールㆍシュトラール,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_メテオールㆍシュトラール,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_メテオールㆍシュトラール,ARG,ARG:1

@SKILL_HIT_EFFECT_メテオールㆍシュトラール,ARG,ARG:1
#DIM ピアッシングチェック
ピアッシングチェック = 0
IF CFLAG:ARG:타인집중플래그 > 0
	IF (MAXBASE:(ARG:1):빙결 < 100 || MAXBASE:(ARG:1):빙결 == 999) && CFLAG:(ARG:1):빙결가드킬 < 1
		CFLAG:(ARG:1):빙결가드킬 = 1
		ピアッシングチェック = 1
	ENDIF
	IF CFLAG:(ARG:1):마법반사플래그 == 1
		CFLAG:(ARG:1):마법반사플래그 = 0
		ピアッシングチェック = 1
	ENDIF
	IF CFLAG:(ARG:1):무효플래그 > 0
		CFLAG:(ARG:1):무효플래그 = 0
		ピアッシングチェック = 1
	ENDIF
	IF CFLAG:(ARG:1):빙결무효화횟수 > 0
		CFLAG:(ARG:1):빙결무효화횟수 = 0
		ピアッシングチェック = 1
	ENDIF
	IF ピアッシングチェック == 1
		PRINTFORML 　　TARGET:[{(ARG:1),2}] %CALLNAME:(ARG:1),20,LEFT%　>>>>>>　상성을 관통했다！
		CFLAG:ARG:타인집중플래그 = 0
	ENDIF
ENDIF

ピアッシングチェック = 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_メテオールㆍシュトラール,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM


CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃




;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_メテオールㆍシュトラール,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_メテオールㆍシュトラール,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_メテオールㆍシュトラール,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_メテオールㆍシュトラール,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_メテオールㆍシュトラール,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_メテオールㆍシュトラール,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"メテオールㆍシュトラール")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_メテオールㆍシュトラール,ARG


PRINTL 적 1체에 빙결 상성의 물리 대 데미지、즉사효과
PRINTL 힘의도눔 시 흡수공격、마의도눔 시 내성관통
PRINTL 유성과도 같은 속도로 적에게 돌격해、전장에서 쫓아버린다。
;没
;@계승부위_メテオールㆍシュトラール,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_メテオールㆍシュトラール,ARG
RETURN 1

;타겟조건
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_メテオールㆍシュトラール,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_メテオールㆍシュトラール,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_メテオールㆍシュトラール,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_メテオールㆍシュトラール,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"メテオールㆍシュトラール"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"メテオールㆍシュトラール"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"メテオールㆍシュトラール"),0

