;スキル名
@SKILL_NAME_劫火の余燼,ARG
RESULTS = 겁화의 여신(余燼)

;　명중
@SKILL_HITRATE_劫火の余燼, ARG
RETURN 100

;　위력
@SKILL_POWER_劫火の余燼, ARG
RETURN 100

;;クリティカル率
;@SKILL_CRITICAL_RATE_劫火の余燼, ARG
;RETURN 5

;;　吸収率
;@SKILL_HP_ABSORBER_劫火の余燼, ARG
;RETURN 0

;　최저공격횟수
@SKILL_MINATTACKNUMBER_劫火の余燼,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_劫火の余燼,ARG
RETURN 1

;;一体辺りの最大ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MAXATK_PER_劫火の余燼,ARG
;RETURN 1

;;一体辺りの最低ＨＩＴ数
;;※ランダム攻撃系に使う
;@SKILL_MINATK_PER_劫火の余燼,ARG
;RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_劫火の余燼, ARG
RETURN 1
;消費なし＝1

;　코스트량
@SKILL_COST_劫火の余燼,ARG
RETURN 0

;　상성
@SKILL_TYPE_劫火の余燼,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_劫火の余燼,ARG
RETURN 0

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_劫火の余燼,ARG
RETURN 2


;　対象が1敵か2味方か3場全体か4特殊か
@SKILL_TARGET_劫火の余燼,ARG
RETURN 4

;　사거리
@SKILL_RANGE_劫火の余燼,ARG
RETURN 3

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_劫火の余燼,ARG
RETURN 1

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_劫火の余燼,ARG
RETURN 6

;　 특수처리 여부 및 처리내용
;@SKILL_SPECIALEFECT_劫火の余燼,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
;SIF ARG == 1
;	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
;@SKILL_SUCCESSION_TYPE_劫火の余燼,ARG
;RETURN 23
;검격

;속도보정
@SKILL_SPEED_劫火の余燼, ARG = -1
RETURN 0

;;　口上の表示とそのタイプ
;@SKILL_MESSAGE_劫火の余燼,ARG


;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_劫火の余燼, ARG = -1
RETURN 0

;没
;@SKILL_RANK_劫火の余燼
;@SKILL_RANKUP_劫火の余燼
;@계승불능_劫火の余燼

;템플릿용、추가효과 시리즈
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_劫火の余燼,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_劫火の余燼,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_劫火の余燼,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_劫火の余燼,ARG
RETURN 0

;;拡散攻撃用追加処理
;;ブレスなどの拡散攻撃に使う
;@SKILL_SPREAD_劫火の余燼,ARG
;RETURN 0

;====================================================
;トリガー発動条件
;====================================================
;@SKILL_TRIGGER_ACTIONABLE_劫火の余燼,ARG,ARG:1
;RETURN 0

;====================================================
;カウンター発動条件
;====================================================
;@SKILL_COUNTER_ACTIONABLE_劫火の余燼,ARG,ARG:1
;RETURN RESULT

;====================================================
;ヒット時ダメージ前処理
;====================================================
;@SKILL_HIT_EFFECT_劫火の余燼,ARG,ARG:1

;====================================================
;ダメージ発動時
;====================================================
;@SKILL_DAMAGE_EFFECT_劫火の余燼,ARG,ARG:1

;====================================================
;行動によるスキルゲージ操作
;====================================================
;@SKILLGAGE_CHARGE_劫火の余燼,ARG,ARG:1
;#DIM SKILL_NUM
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"劫火の余燼")
;
;IF ARG == ARG:1
;	IF SKILLGAGE_H_GET(ARG,SKILL_NUM) > 0 && SKILLGAGE_D_GET(ARG,SKILL_NUM) > 0
;		IF SKILLGAGE_F_GET(ARG,SKILL_NUM) == 0
;			CALL SKILLGAGE_H_SET,ARG,SKILL_NUM,0
;			CALL SKILLGAGE_F_SET,ARG,SKILL_NUM,0
;			CFLAG:ARG:불사신플래그 = 0
;			CFLAG:ARG:회복불능플래그 = 0
;			CFLAG:ARG:이악물기플래그 = 1
;			PRINTFORMW %CALLNAME:ARG%を奮い立たせる余燼が燃え尽きたㆍㆍㆍ
;			CALL DEAD_CHARA,ARG
;		ENDIF
;	ENDIF
;ENDIF

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_劫火の余燼,ARG,ARG:1,SKILL_NUM
#DIM SKILL_NUM

;CALL ATTACK_SINGLE,ARG,POS(ARG:1),SKILL_NUM;	単体攻撃
;CALL ATTACK_FIELD,ARG,ARG:1,SKILL_NUM;			範囲攻撃(列、全体)
;CALL ATTACK_SPREAD,ARG,ARG:1,SKILL_NUM;		拡散攻撃
;CALL ATTACK_RAND,ARG,ARG:1,SKILL_NUM;			ランダム攻撃


;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;전투중 이외에서의 실행
;====================================================
;@ACTION_FIELD_劫火の余燼,ARG,ARG:1,SKILL_NUM
;#DIM SKILL_NUM

;CALL HEAL_SINGLE,ARG,POS(ARG:1),SKILL_NUM	;単体回復
;CALL HEAL_FIELD,ARG,ARG:1,SKILL_NUM		;範囲回復(列、全体)
;CALL ALL_HEAL, POS(ARG:1), 0				;単体前回復

;====================================================
;실행내용 여기까지
;====================================================

;====================================================
;追加コマンド(EXTRAの下)
;====================================================
;@SKILL_SPECIAL_COMMAND_EXTRA_劫火の余燼,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(MAGICの下、魔封だと消える)
;====================================================
;@SKILL_SPECIAL_COMMAND_MAGIC_劫火の余燼,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;追加コマンド(一番下)
;====================================================
;@SKILL_SPECIAL_COMMAND_劫火の余燼,ARG,LOCAL,SKILL_NUM
;#DIM SKILL_NUM
;#LOCALSIZE 1
;PRINTFORML [{LOCAL}] 剣質変化
;C:(LOCAL++) = 10000 + SKILL_NUM;←必須
;RETURN LOCAL;←必須

;====================================================
;コマンド実行
;====================================================
;@INPUT_COM_劫火の余燼,ARG
;RETURN -1
;-1 再びコマンド選択へ
; 1 キャラ選択へ

;====================================================
;戦闘中スキルゲージの可視化
;====================================================
;@SKILLGAGE_DISPLAY_劫火の余燼,ARG
;SKILL_NUM = PU_SKILLNUM_GET(ARG,"劫火の余燼")
;SKILLGAGE_D_GET(ARG,SKILL_NUM)


;　説明文の表示
@SKILL_EXPLAIN_劫火の余燼,ARG
#DIM SKILL_NUM
SKILL_NUM = PU_SKILLNUM_GET(ARG,"劫火の余燼")
PRINTFORML 주는 데미지가 항상 {CSTR:ARG:104 != "" ? 20 # 12}％ 증가。
PRINTFORML 치사데미지 피탄시 이악묾과 동시에 ＨＰ가 1로 고정되고、DYING과 STONE의 상태이상을 차단한다。
PRINTFORML 발동부터 {CSTR:ARG:104 != "" ? 2 # 1}라운드 후、강제적으로 전투불능이 된다。
PRINTL 이 효과는 이악물기와 불굴의투지 보다 우선시된다。

;没
;@계승부위_劫火の余燼,ARG

;사용조건
@SKILL_SPECIAL_ACTIONABLE_劫火の余燼,ARG
RETURN 1

;ターゲット条件
;↓のSKILL_T_RESERVEがRETURN 1 の時に使用する
;@SKILL_SPECIAL_TARGET_劫火の余燼,USE_CHARA,ARG
;#DIM USE_CHARA
;RETURN 0

;控えに使えるか
;@SKILL_T_RESERVE_劫火の余燼,ARG
;RETURN 0

;어떤 배드스테이터스를 고칠수 있는가
;@SKILL_CURE_STATE_劫火の余燼,USE_CHARA,ARG
;#DIM USE_CHARA
;SELECTCASE GET_STATE(ARG)
;	CASE "HAPPY","CLOSE"
;		RETURN 1
;ENDSELECT
;RETURN 0

;この技を複数持てるか
;@중복습득수_劫火の余燼,ARG
;RETURN 1

;スキル重複で役に立つ関数
;HAVE_SKILL_OVERLAP(ARG,PU_SKILLNUM_GET(ARG,"劫火の余燼"))

;スキルゲージで役に立つ関数
;SKILLGAGE_H_GET(ARG,PU_SKILLNUM_GET(ARG,"劫火の余燼"))
;CALL SKILLGAGE_H_SET,ARG,PU_SKILLNUM_GET(ARG,"劫火の余燼"),0

;====================================================
;独自処理
;====================================================
;（予定）と付けられている方は仮置きの物。実際に採用されるかどうかはわからない。
@AUTO_PU_SKILL_劫火の余燼,ARG,ARGS = ""
#FUNCTION
#DIM SKILL_NUM
;ARG = 使用者
;ARGS （데미지↑、피데미지↑、명중↑、회피↑、魔威↑、魔効↑、etc…）といった感じの奴
;文字列を参考にする都合上、どの文字列を使うか事前に定めておいた方がいいかも
;CSTR:ARG:100 = EXTRA勢出典作品、『アークナイツ』とでも書き込む（予定）
;CSTR:ARG:101 = EXTRA勢出典作品でどのキャラなのか、とりあえず『スルト（アークナイツ）』とでも書き込む（予定）
;CSTR:ARG:102 = 専用スキルその1のスキルランク変数として使用、この場合は『レーヴァテイン（ＡＮ）』が対象
;CSTR:ARG:103 = 専用スキルその2のスキルランク変数として使用、この場合は『災厄の巨焔』が対象
;CSTR:ARG:104 = 専用スキルその3のスキルランク変数として使用、この場合は『ラグナロク（ＡＮ）』が対象
SKILL_NUM = PU_SKILLNUM_GET(ARG,"劫火の余燼")
IF HAVE_PU_SKILL(ARG,"劫火の余燼") > 0
	SELECTCASE ARGS
		CASE "ダメージ↑"
			IF CSTR:ARG:104 != ""
				RETURNF 20
			ELSE
				RETURNF 12
			ENDIF
		CASE "食いしばり"
			SIF SKILLGAGE_H_GET(ARG,SKILL_NUM) < 1
				RETURNF 1
		CASE "状態異常無効"
			SIF SKILLGAGE_D_GET(ARG,SKILL_NUM) > 0
				RETURNF 1
		CASE "強制即死"
			SIF SKILLGAGE_H_GET(ARG,SKILL_NUM) > 0 && SKILLGAGE_D_GET(ARG,SKILL_NUM) == 0
				RETURNF 1
	ENDSELECT
ENDIF