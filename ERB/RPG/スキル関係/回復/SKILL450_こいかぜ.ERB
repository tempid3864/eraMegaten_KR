;元Never More

;스킬명
@SKILL_NAME_450,ARG
RESULTS = 코이카제

;　명중
@SKILL_HITRATE_450, ARG
RETURN 100

;　위력
@SKILL_POWER_450, ARG
SIF ARG == -1
	RETURN 200
IF ABL:ARG:가창기능 == 0
	RETURN 100
ELSEIF ABL:ARG:가창기능 == 1
	RETURN 110
ELSEIF ABL:ARG:가창기능 == 2
	RETURN 120
ELSEIF ABL:ARG:가창기능 == 3
	RETURN 130
ELSEIF ABL:ARG:가창기능 == 4
	RETURN 140
ELSEIF ABL:ARG:가창기능 == 5
	RETURN 150
ELSEIF ABL:ARG:가창기능 == 6
	RETURN 160
ELSEIF ABL:ARG:가창기능 == 7
	RETURN 170
ELSEIF ABL:ARG:가창기능 == 8
	RETURN 180
ELSEIF ABL:ARG:가창기능 == 9
	RETURN 190
ELSE
	RETURN 200
ENDIF
	RETURN 200

;　최저공격횟수
@SKILL_MINATTACKNUMBER_450,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_450,ARG
RETURN 1

;　2 ＨＰ지불　3 ＭＰ지불
@SKILL_COSTTYPE_450, ARG, ARG:1
RETURN 3
;소비없음＝1

;　코스트량
@SKILL_COST_450, ARG
    RETURN 35

;　상성
@SKILL_TYPE_450,ARG
RETURN 0

;　1 EXTRA　2 MAGIC
@SKILL_DECIDE_TYPE_450, ARG
RETURN 2
;声は魔法

;　1 물리데미지로 계산　2 마법데미지로 계산
@SKILL_DAMAGETYPE_450, ARG
RETURN 0


;　대상　1 적　2 아군　3 양쪽전체　4 특수
@SKILL_TARGET_450
RETURN 2

;　사거리
@SKILL_RANGE_450,ARG
RETURN 1

;　범위　1 1체　2 1열　3 전체　4 적아군전체
@SKILL_SPHERE_450,ARG
RETURN 3

;　 효과타입　1 공격、2 회복、3 상태변화、4 특수、5 상태회복
@SKILL_EFECT_450
RETURN 2

;　 특수처리 여부 및 처리내용
@SKILL_SPECIALEFECT_450,ARG
;ARG = 1인 경우는 처리 여부를 반환합니다
SIF ARG == 1
	RETURN 0
;아래쪽에 특수처리를 기술합니다

;계승타입
@SKILL_SUCCESSION_TYPE_450
RETURN 4

@계승불능_450
RETURN 1
;継承不能

;속도보정
@SKILL_SPEED_450, ARG = -1
RETURN 0

;　구상의 표시와 그 타입
@SKILL_MESSAGE_450,ARG

;전투시 이외에 사용 가능한가
@SKILL_ACTIONABLE_FIELD_450
RETURN 1

;스킬변화？용 랭크
@SKILL_RANK_450
RETURN 22

@SKILL_RANKUP_450
RETURN 0

;-----------------------------
;- 추가효과処理
;-----------------------------
;추가효과 스테이터스
@SKILL_ADDTIONAL_STATE_450,ARG
RETURN 0

;추가효과 상성
@SKILL_ADDTIONAL_TYPE_450,ARG
RETURN 0

;추가효과 기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_450,ARG
RETURN 0

;추가효과 부여확률상한
@SKILL_ADDTIONAL_MAX_RATE_450,ARG
RETURN 0

;====================================================
;스킬 및 행동의 실행
;====================================================
@ACTION_450,ARG,ARG:1
;コスト支払い
;CALL PAY_COST, ARG, 450

;ペンパトラムリタ部分
IF ARG:1 == 19
	LOCAL:7 = 1
	LOCAL:8 = 7
ELSE
	LOCAL:7 = 7
	LOCAL:8 = 17
ENDIF

FOR LOCAL:9,LOCAL:7,LOCAL:8
	LOCALS = 포지션{LOCAL:9}
	SIF FLAG:LOCALS < 0
		CONTINUE
	LOCAL = FLAG:LOCALS
	CALL SKILL_CURE_STATE_409,CFLAG:LOCAL:상태이상
	IF RESULT == 1
		SIF GET_STATE(CFLAG:LOCAL:상태이상) == "PANIC" || GET_STATE(CFLAG:LOCAL:상태이상) == "CHARM"
			CFLAG:LOCAL:혼란매료리커버 = 1
		PRINTFORML 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(LOCAL),20,LEFT%　>>>>>>　제정신으로 돌아왔다!
		CFLAG:LOCAL:상태이상 = 0
		CFLAG:LOCAL:상태이상경과턴수 = 0
	ENDIF
	;メディアラマ部分
	CALL HEAL_SINGLE,ARG,LOCAL,450
NEXT
WAIT



;====================================================
;전투중 이외에서의 실행
;====================================================
@ACTION_FIELD_450,ARG,ARG:1
;コスト支払い
;CALL VAR_MP,ARG,-35,0

FOR LOCAL:9,LOCAL:7,LOCAL:8
	LOCALS = 포지션{LOCAL:9}
	SIF FLAG:LOCALS < 0
		CONTINUE
	LOCAL = FLAG:LOCALS
	CALL SKILL_CURE_STATE_450,CFLAG:LOCAL:상태이상
	IF RESULT == 1
		SIF GET_STATE(CFLAG:LOCAL:상태이상) == "PANIC" || GET_STATE(CFLAG:LOCAL:상태이상) == "CHARM"
			CFLAG:LOCAL:혼란매료리커버 = 1
		PRINTFORML 　　TARGET:[{LOCAL:9,2}] %CALLNAME:(LOCAL),20,LEFT%　>>>>>>　제정신으로 돌아왔다!
		CFLAG:LOCAL:상태이상 = 0
		CFLAG:LOCAL:상태이상경과턴수 = 0
	ENDIF
	CALL HEAL_SINGLE,ARG,LOCAL,450
NEXT
WAIT

;====================================================
;실행내용 여기까지
;====================================================

;　 설명문 표시
@SKILL_EXPLAIN_450
PRINTL 라이브「코이카제」로 아군 전체를 회복하고, 가창기능에 따라 다양한 상태이상을 치료한다.

;계승에 필요한 부위
@계승부위_450,ARG
RETURN 0

;사용조건
@SKILL_SPECIAL_ACTIONABLE_450,ARG
CALL CHECK_COST,ARG,450
SIF RESULT == 0
	RETURN 0
RETURN 1
;とくにない



;何のバッドステータスを治せるか
;技能 0：HAPPY,CHARM
;技能 1：HAPPY,CHARM
;技能 2：HAPPY,CHARM,PANIC
;技能 3：HAPPY,CHARM,PANIC
;技能 4：HAPPY,CHARM,PANIC,SLEEP
;技能 5：HAPPY,CHARM,PANIC,SLEEP
;技能 6：HAPPY,CHARM,PANIC,SLEEP,CLOSE
;技能 7：HAPPY,CHARM,PANIC,SLEEP,CLOSE
;技能 8：HAPPY,CHARM,PANIC,SLEEP,CLOSE,PALYZE
;技能 9：HAPPY,CHARM,PANIC,SLEEP,CLOSE,PALYZE
;技能10：HAPPY,CHARM,PANIC,SLEEP,CLOSE,PALYZE,CURSE

@SKILL_CURE_STATE_450,ARG
;U＝使用者

IF U == -1
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP","CLOSE","PALYZE","CURSE"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 0
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 1
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM",
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 2
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 3
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 4
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 5
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 6
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP","CLOSE"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 7
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP","CLOSE"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 8
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP","CLOSE","PALYZE"
			RETURN 1
	ENDSELECT
ELSEIF ABL:U:가창기능 == 9
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP","CLOSE","PALYZE"
			RETURN 1
	ENDSELECT
ELSE
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM","PANIC","SLEEP","CLOSE","PALYZE","CURSE"
			RETURN 1
	ENDSELECT
ENDIF
	SELECTCASE GET_STATE(ARG)
		CASE "HAPPY","CHARM"
			RETURN 1
	ENDSELECT

RETURN 0


